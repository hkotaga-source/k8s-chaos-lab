apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-example
  namespace: chaos-mesh
spec:
  mode: one
  duration: "2m"
  selector:
    namespaces:
      - default
    labelSelectors:
      app: sample-app
  stressors:
    cpu:
      workers: 2
      load: 80
